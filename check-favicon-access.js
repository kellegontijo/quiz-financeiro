// Script para verificar se os arquivos de favicon estão acessíveis\nconst fs = require('fs');\nconst path = require('path');\n\n// Verificar se os arquivos de favicon existem na pasta public\nconst publicDir = path.join(__dirname, 'public');\nconst buildDir = path.join(__dirname, 'build');\n\nconsole.log('Verificando arquivos de favicon na pasta public:');\nconst faviconFiles = [\n  'favicon.ico',\n  'favicon-16x16.png',\n  'favicon-32x32.png',\n  'apple-touch-icon.png'\n];\n\nfaviconFiles.forEach(file => {\n  const publicPath = path.join(publicDir, file);\n  const buildPath = path.join(buildDir, file);\n  \n  const existsInPublic = fs.existsSync(publicPath);\n  const existsInBuild = fs.existsSync(buildPath);\n  \n  console.log(`- ${file}:`);\n  console.log(`  Public: ${existsInPublic ? 'Encontrado' : 'Não encontrado'}`);\n  console.log(`  Build:  ${existsInBuild ? 'Encontrado' : 'Não encontrado'}`);\n});\n\nconsole.log('\\nVerificação concluída!');